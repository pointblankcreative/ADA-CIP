[project]
name = "cip-backend"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.uvicorn]
reload-excludes = [".venv", "node_modules", "frontend", ".git", "__pycache__"]
